{
  "dependencies": {
    "@calcit/procs": "^0.7.0-a3"
  },
  "scripts": {
    "try-js": "mode=ci cr --emit-js -1 && yarn && mode=ci node ./main.mjs",
    "watch-server": "cr --emit-js"
  },
  "devDependencies": {},
  "version": "0.0.7"
}
